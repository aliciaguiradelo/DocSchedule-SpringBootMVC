const http = require("http");

exports.clearTerminal = async (PORT = 8443) => {
	return new Promise((resolve) => {
		http
			.get(`http://localhost:${PORT}`, () => resolve())
			.on("error", () => resolve()); //dont do anything here, for people who don't use vscode
	});
};
